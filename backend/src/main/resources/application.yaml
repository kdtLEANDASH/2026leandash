spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

    url: jdbc:mysql://localhost:3306/leandash_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root        # 👈 형 MySQL 아이디 (보통 root)
    password: 4144  # 👈 형 MySQL 비밀번호로 바꿔!

  jpa:
    hibernate:

      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true